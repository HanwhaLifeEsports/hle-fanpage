# 전부 선택 사항이다. 하나도 없어도 사이트는 뜬다.
# 실제 값은 .env.local 에 넣는다 (git 에 올라가지 않는다).

# 배포 도메인. OG 이미지와 사이트맵의 절대경로가 이 값으로 잡힌다.
# Vercel 에서는 VERCEL_PROJECT_PRODUCTION_URL 이 자동으로 들어와 생략해도 된다.
NEXT_PUBLIC_SITE_URL=https://example.com

# ---------------------------------------------------------------------
# 팬 사진 저장소 (Supabase)
# ---------------------------------------------------------------------
# 없으면 팬 사진이 브라우저(IndexedDB)에만 저장된다. 화면은 똑같이 동작하지만
# 하트가 그 기기에서만 세어져서 "하트 1위를 카드에" 가 성립하지 않는다.
#
# 붙이는 순서:
#   1. supabase.com 에서 프로젝트를 만든다. 리전은 Northeast Asia (Seoul).
#      개인정보를 국내에 두는 편이 처리방침이 단순해진다.
#   2. Authentication > Providers > Anonymous sign-ins 를 켠다.
#      로그인 없이 하트 중복을 막으려면 사람마다 고정된 id 가 필요하다.
#   3. SQL Editor 에 supabase/schema.sql 을 통째로 붙여 넣고 실행한다.
#   4. Project Settings > API 에서 아래 두 값을 복사한다.
#
# anon 키는 공개되는 값이다. 숨겨야 하는 열쇠가 아니라 "여기부터는 RLS 가
# 판단한다" 는 표시에 가깝다. 실제 권한은 전부 supabase/schema.sql 에 있다.
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ---------------------------------------------------------------------
# 권리 침해 신고 수령인
# ---------------------------------------------------------------------
# 저작권법 제103조 제4항이 지정과 공지를 요구한다. 비워 두면 저장소 이슈로
# 안내되는데, 이슈는 공개된 자리라 권리자가 신원을 드러내지 않고 요청하기 어렵다.
NEXT_PUBLIC_ABUSE_EMAIL=

# ---------------------------------------------------------------------
# 되돌리기 스위치
# ---------------------------------------------------------------------
# off 로 두면 선수 사진(운영자 사진 + 팬 사진 + 업로드)이 전부 화면에서 빠진다.
# 초상권 관련 요청이 왔을 때 코드를 고치지 않고 즉시 내리기 위한 것이다.
NEXT_PUBLIC_PLAYER_PHOTOS=

# 치지직 자체 플레이어. off 로 두면 채널 링크로만 안내한다.
NEXT_PUBLIC_CHZZK_PLAYER=

# LoL Esports API 키. 비워 두면 공개 클라이언트 키를 쓴다.
LOLESPORTS_API_KEY=
